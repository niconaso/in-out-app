import { SortInOutPipe } from './sort-in-out.pipe';

describe('SortInOutPipe', () => {
  it('create an instance', () => {
    const pipe = new SortInOutPipe();
    expect(pipe).toBeTruthy();
  });
});
